v1.0.4
-Permut_GRanges internally is now mostly tidy, fixed some critical bugs towards double-counting
-plyranges as dependency

v1.0.3
-modified Permut_GRanges code leading to massive (5-10x) speed-up depending on nperm parameter
without and mclapply on a single thread

v1.0.2
-added restrictset argument to Permut_GRanges which can limit to allowed overlaps between the sets, e.g. this can be TAD boundaries

v1.0.1
-added run_apeglm function and added apeglm + DESeq2 as suggested dependencies, but not as depends to avoid pulling many packages when installing

v1.0.0
-initial Github commit